Any folder that starts with `x_` should be setup with a `.htpasswd`.

These folders include but are not limited to:

`x_admin` - For Admin panel to server.

`x_testing` - For testing non-production website.
